import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  btnIcon: {
    height: 51.75,
    width: 55,
  },
  btnContainer: {
    paddingTop: 10,
  },
});

export default styles;
