import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  movies: {
    flex: 1,
    paddingLeft: 16,
    paddingRight: 16,
    backgroundColor: "#FFF",
  },
  moviesTitle: {
    fontSize: 32,
    fontWeight: "bold",
    paddingTop: 16,
    paddingBottom: 16,
  },
});

export default styles;
